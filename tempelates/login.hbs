<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Professional Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        /* General Styling */
        body {
            font-family: 'Poppins', sans-serif;
            background: black; /* Dark background */
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
            overflow: hidden;
            position: relative;
            animation: backgroundAnimation 10s ease-in-out infinite;
        }

        /* Animated gradient background */
        @keyframes backgroundAnimation {
            0% {
                background: linear-gradient(135deg, #0f0f0f, #004d7a);
            }
            50% {
                background: linear-gradient(135deg, #005c4b, #002244);
            }
            100% {
                background: linear-gradient(135deg, #0f0f0f, #004d7a);
            }
        }

        /* Container with glassy effect */
        .container {
            background: rgba(0, 0, 0, 0.7);
            padding: 40px;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.8);
            position: relative;
            z-index: 1;
            width: 400px;
        }

        /* Logo styling */
        .logo {
            display: block;
            margin: 0 auto 20px;
            width: 80px;
            opacity: 0.9;
            animation: pulse 2s infinite;
        }

        /* Title styling */
        h1 {
            text-align: center;
            font-size: 2.5em;
            color: #00ffcc;
            margin-bottom: 20px;
            animation: fadeIn 2s ease-out;
        }

        /* Input fields */
        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 15px;
            margin: 10px 0;
            border: none;
            border-radius: 10px;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            font-size: 1.1em;
            outline: none;
            box-shadow: 0 0 15px rgba(0, 255, 255, 0.3);
            transition: all 0.3s ease;
        }

        /* Input focus effects */
        input[type="text"]:focus,
        input[type="password"]:focus {
            box-shadow: 0 0 20px rgba(0, 255, 255, 0.7);
            background: rgba(255, 255, 255, 0.3);
        }

        /* Submit button */
        input[type="submit"] {
            width: 100%;
            padding: 15px;
            border: none;
            border-radius: 10px;
            background: linear-gradient(135deg, #00ffcc, #00aaff);
            color: white;
            font-size: 1.3em;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.5);
        }

        /* Hover effect for submit button */
        input[type="submit"]:hover {
            background: linear-gradient(135deg, #00aaff, #00ffcc);
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.7);
        }

        /* Link styles */
        .links {
            display: flex;
            justify-content: space-between;
            margin-top: 20px;
        }

        .links a {
            color: #00ffcc;
            text-decoration: none;
            font-size: 0.9em;
            transition: color 0.3s ease;
        }

        .links a:hover {
            color: #00aaff;
        }

        /* Pulse animation for the logo */
        @keyframes pulse {
            0% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.1);
            }
            100% {
                transform: scale(1);
            }
        }

        /* Fade-in effect for text */
        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(-30px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Floating particles for VFX */
        .particles {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        .particle {
            position: absolute;
            width: 10px;
            height: 10px;
            background: #00ffcc;
            border-radius: 50%;
            animation: float 10s ease-in-out infinite;
        }

        @keyframes float {
            0% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-50px);
            }
            100% {
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>

    <!-- Particle effect background -->
    <div class="particles">
        <!-- Randomly positioned particles -->
        <div class="particle" style="top: 20%; left: 10%; animation-delay: 0s;"></div>
        <div class="particle" style="top: 50%; left: 30%; animation-delay: 2s;"></div>
        <div class="particle" style="top: 70%; left: 50%; animation-delay: 4s;"></div>
        <div class="particle" style="top: 30%; left: 80%; animation-delay: 1s;"></div>
        <div class="particle" style="top: 60%; left: 60%; animation-delay: 3s;"></div>
    </div>

    <!-- Login form container -->
    <div class="container">
        <img src="VoxView" alt="VoxView" class="be quit">
        <h1>Login</h1>
        <form action="/login" method="post">
            <input type="text" placeholder="Username" name="name" required>
            <input type="password" placeholder="Password" name="password" required>
            <input type="submit" value="Login">
            <div class="links">
                <a href="/signup">Don't have an account? Sign Up</a>
                <a href="/forgot">Forgot Password?</a>
            </div>
        </form>
    </div>

</body>
</html>
